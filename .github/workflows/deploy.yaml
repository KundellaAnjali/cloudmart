name: CloudMart Deploy

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:

  # ---------------------------
  # NETWORK STACK
  # ---------------------------

  lint-network:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Lint Network Stack
        run: |
          cfn-lint --non-zero-exit-code error cloudformation/network-stack.yaml

  deploy-network:
    needs: lint-network
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.ARN_ROLE }}
          aws-region: ap-south-1

      - name: Validate Network Stack
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/network-stack.yaml

      - name: Deploy Network Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/network-stack.yaml \
            --stack-name cloudmart-network-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

  # ---------------------------
  # DATA STACK
  # ---------------------------

  lint-data:
    needs: deploy-network
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Lint Data Stack
        run: |
          cfn-lint --non-zero-exit-code error cloudformation/data-stack.yaml

  deploy-data:
    needs: lint-data
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.ARN_ROLE }}
          aws-region: ap-south-1

      - name: Validate Data Stack
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/data-stack.yaml

      - name: Deploy Data Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/data-stack.yaml \
            --stack-name cloudmart-data-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

      - name: Check Data Stack Status
        run: |
          aws cloudformation describe-stacks \
            --stack-name cloudmart-data-stack

      - name: Show Data Stack Events On Failure
        if: failure()
        run: |
          aws cloudformation describe-stack-events \
            --stack-name cloudmart-data-stack

##IAM stack

  lint-iam:
    needs: deploy-data
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Lint IAM Stack
        run: |
          cfn-lint --non-zero-exit-code error cloudformation/iam-stack.yaml

  deploy-iam:
    needs: lint-iam
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.ARN_ROLE }}
          aws-region: ap-south-1

      - name: Verify AWS Connection
        run: aws sts get-caller-identity

      - name: Validate IAM Stack
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/iam-stack.yaml

      - name: Deploy IAM Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/iam-stack.yaml \
            --stack-name cloudmart-iam-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

      - name: Check IAM Stack Status
        run: |
          aws cloudformation describe-stacks \
            --stack-name cloudmart-iam-stack

      - name: Show IAM Stack Events On Failure
        if: failure()
        run: |
          aws cloudformation describe-stack-events \
            --stack-name cloudmart-iam-stack
# auth-stack


  # ---------------------------
# AUTH STACK (SAM)
# ---------------------------

  # ---------------------------
# AUTH STACK
# ---------------------------

  lint-auth:
    needs: deploy-iam
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Lint Auth Stack
        run: |
          cfn-lint --non-zero-exit-code error cloudformation/auth-stack.yaml

  deploy-auth:
    needs: lint-auth
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.ARN_ROLE }}
          aws-region: ap-south-1

      - name: Setup SAM CLI
        uses: aws-actions/setup-sam@v2

      - name: Validate Auth Stack
        run: |
          sam validate --template-file cloudformation/auth-stack.yaml

      - name: Build Auth Stack
        run: |
          sam build \
            --template-file cloudformation/auth-stack.yaml

      - name: Deploy Auth Stack
        run: |
          sam deploy \
            --template-file .aws-sam/build/template.yaml \
            --stack-name cloudmart-auth-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --resolve-s3 \
            --no-fail-on-empty-changeset \
            --region ap-south-1

#--------- Product --------------------
  # ---------------------------
# PRODUCT STACK
# ---------------------------

  lint-product:
    needs: deploy-auth
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install cfn-lint
        run: pip install cfn-lint

      - name: Lint Product Stack
        run: |
          cfn-lint --non-zero-exit-code error cloudformation/product-stack.yaml

    deploy-product:
      needs: lint-product
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v4

        - name: Configure AWS Credentials
          uses: aws-actions/configure-aws-credentials@v4
          with:
            role-to-assume: ${{ secrets.ARN_ROLE }}
            aws-region: ap-south-1

        - name: Setup SAM CLI
          uses: aws-actions/setup-sam@v2

        - name: Validate Product Stack
          run: |
            sam validate \
              --template-file cloudformation/product-stack.yaml

        - name: Build Product Stack
          run: |
            sam build \
              --template-file cloudformation/product-stack.yaml

        - name: Deploy Product Stack
          run: |
            sam deploy \
              --template-file .aws-sam/build/template.yaml \
              --stack-name cloudmart-product-stack \
              --capabilities CAPABILITY_NAMED_IAM \
              --resolve-s3 \
              --no-fail-on-empty-changeset \
              --region ap-south-1